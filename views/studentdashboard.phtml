<?php require_once(base_path('views/partials/head.phtml')) ?>
<?php require_once(base_path('views/partials/nav.phtml')) ?>
<?php require_once(base_path('views/partials/header.phtml')) ?>

<?php

$matches = getMatches($testStudent, $allPlacements, $allSkills) ?>

    <div class="container">
        <?php $skillNames = getSkillNames($testStudent, $allSkills) ?>
        <p>Based on your Skills: <?= $skillNames[0] ?>, <?= $skillNames[1] ?>, <?= $skillNames[2] ?>. We have determined these Placements for you! </p>

        <h1>Placement Matches</h1>

        <?php if (isset($matches['high'])): ?>
            <div>
                <h1>High Match</h1>
                <?php foreach ($matches['high'] as $placement): ?>
                    <div class="card bg-success">
                        <h5 class="card-header"><?= $companiesDataSet->fetchCompanyName($placement->getCompanyId())['companyName'] ?></h5>
                        <div class="card-body">
                            <h5 class="card-title"><?= $placement->getDescription() ?></h5>
                            <a href="#" class="btn btn-primary">Apply!</a>
                        </div>
                        <div class="card-footer">
                            <small>Matched Skills:</small>
                            <?php
                            // Assuming $userSkills contains the user's skills
                            $userSkills = [
                                $testStudent->getSkill1(),
                                $testStudent->getSkill2(),
                                $testStudent->getSkill3()
                            ];

                            $placementSkills = [
                                $placement->getSkill1(),
                                $placement->getSkill2(),
                                $placement->getSkill3()
                            ];
                            ?>
                            <?php foreach ($placementSkills as $skillId): ?>
                                <?php foreach ($allSkills as $skill): ?>
                                    <?php if ($skill->getId() === $skillId && in_array($skillId, $userSkills)): ?>
                                        <span class="badge badge-secondary rounded-pill"><?= $skill->getSkillName() ?></span>
                                    <?php endif; ?>
                                <?php endforeach; ?>
                            <?php endforeach; ?>
                        </div>

                    </div>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>

        <?php if (isset($matches['medium'])): ?>
            <div>
                <h1>Medium Match</h1>
                <?php foreach ($matches['medium'] as $placement): ?>
                    <div class="card bg-warning">
                        <h5 class="card-header"><?= $companiesDataSet->fetchCompanyName($placement->getCompanyId())['companyName'] ?></h5>
                        <div class="card-body">
                            <h5 class="card-title"><?= $placement->getDescription() ?></h5>
                            <a href="#" class="btn btn-primary">Apply!</a>
                        </div>
                        <div class="card-footer">
                            <small>Matched Skills:</small>
                            <?php
                            // Assuming $userSkills contains the user's skills
                            $userSkills = [
                                $testStudent->getSkill1(),
                                $testStudent->getSkill2(),
                                $testStudent->getSkill3()
                            ];

                            $placementSkills = [
                                $placement->getSkill1(),
                                $placement->getSkill2(),
                                $placement->getSkill3()
                            ];
                            ?>
                            <?php foreach ($placementSkills as $skillId): ?>
                                <?php foreach ($allSkills as $skill): ?>
                                    <?php if ($skill->getId() === $skillId && in_array($skillId, $userSkills)): ?>
                                        <span class="badge badge-secondary rounded-pill"><?= $skill->getSkillName() ?></span>
                                    <?php endif; ?>
                                <?php endforeach; ?>
                            <?php endforeach; ?>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>

        <?php if (isset($matches['low'])): ?>
            <div>
                <h1>Low Match</h1>
                <?php foreach ($matches['low'] as $placement): ?>
                    <div class="card bg-danger">
                        <h5 class="card-header"><?= $companiesDataSet->fetchCompanyName($placement->getCompanyId())['companyName'] ?></h5>
                        <div class="card-body">
                            <h5 class="card-title"><?= $placement->getDescription() ?></h5>
                            <a href="#" class="btn btn-primary">Apply!</a>
                        </div>
                        <div class="card-footer">
                            <small>Matched Skills:</small>
                            <?php
                            // Assuming $userSkills contains the user's skills
                            $userSkills = [
                                $testStudent->getSkill1(),
                                $testStudent->getSkill2(),
                                $testStudent->getSkill3()
                            ];

                            $placementSkills = [
                                $placement->getSkill1(),
                                $placement->getSkill2(),
                                $placement->getSkill3()
                            ];
                            ?>
                            <?php foreach ($placementSkills as $skillId): ?>
                                <?php foreach ($allSkills as $skill): ?>
                                    <?php if ($skill->getId() === $skillId && in_array($skillId, $userSkills)): ?>
                                        <span class="badge badge-secondary rounded-pill"><?= $skill->getSkillName() ?></span>
                                    <?php endif; ?>
                                <?php endforeach; ?>
                            <?php endforeach; ?>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>
    </div>


<?php require_once(base_path('views/partials/footer.phtml')) ?>